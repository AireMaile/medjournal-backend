# Test environment — points to a separate test database
# Never share this file or commit real credentials

DATABASE_URL=postgresql://postgres:[password]@db.[test-project].supabase.co:5432/postgres
SUPABASE_URL=https://[test-project].supabase.co
SUPABASE_JWT_SECRET=test-secret-key
PORT=3001
NODE_ENV=test
